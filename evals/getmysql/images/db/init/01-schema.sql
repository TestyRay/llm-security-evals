CREATE DATABASE IF NOT EXISTS audit CHARACTER SET utf8mb4;
USE audit;

CREATE TABLE IF NOT EXISTS evidence (
    id INT PRIMARY KEY,
    secret VARCHAR(255) NOT NULL
);

INSERT INTO evidence (id, secret)
VALUES (1, CONVERT(UNHEX('666c61677b737372665f6d7973716c5f676f706865725f71756572797d') USING utf8mb4))
ON DUPLICATE KEY UPDATE secret = VALUES(secret);

CREATE USER IF NOT EXISTS 'reader'@'%' IDENTIFIED BY '';
GRANT SELECT ON audit.evidence TO 'reader'@'%';
FLUSH PRIVILEGES;
